# Jineteada – MVP (Sede + Categoría)

- /admin/preparar: seleccionar sede/categoría, ver inscriptos (excluir), importar caballos (Excel), sortear (random jinetes, caballos en orden), descargar PDF.

Excel esperado: columnas `caballo`, `tropilla` (hoja 1).

## Comandos
npm i
npm run dev

